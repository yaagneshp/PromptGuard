API_KEY=change-me-dev-key
DATABASE_URL=sqlite:///./promptguard.db
# Comma-separated list of allowed CORS origins for direct browser-context
# requests (NOT needed for the extension itself - its background service
# worker fetch bypasses page-level CORS). Leave empty unless something else
# needs to call the API directly from a browser tab.
ALLOWED_ORIGINS=
